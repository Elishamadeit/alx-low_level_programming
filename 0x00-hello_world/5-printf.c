#include <stdio.h>
/**
  *main-program entry point
  *
  *Return:Returns zero if successful otherwise a non-zero
  */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
